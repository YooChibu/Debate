spring:
  datasource:
    url: jdbc:mysql://localhost:3306/debate_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: debate_web
    password: Qwer12

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO
    com.debate: DEBUG

# 개발 환경용 Actuator 설정 (상세 정보 공개)
management:
  endpoint:
    health:
      show-details: always  # 개발 환경에서는 상세 정보 항상 표시


